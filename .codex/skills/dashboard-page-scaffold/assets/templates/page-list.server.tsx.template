import { SpotifyAttribution } from "@/components/spotify-stats-logo";
import { {{ListComponentName}} } from "@/components/{{listComponentFile}}";
import { {{helperFunctionName}} } from "@/lib/spotify/helpers";

export default async function {{PageComponentName}}() {
  const itemsByTimeRange = await {{helperFunctionName}}({{limit}});

  return (
    <div className="space-y-6">
      <div className="flex items-start justify-between">
        <div>
          <h1 className="text-2xl font-bold tracking-tight">{{pageTitle}}</h1>
          <p className="text-muted-foreground">{{pageDescription}}</p>
        </div>
        <SpotifyAttribution className="hidden md:flex" />
      </div>

      <{{ListComponentName}} {{itemsPropName}}={itemsByTimeRange} />
    </div>
  );
}

